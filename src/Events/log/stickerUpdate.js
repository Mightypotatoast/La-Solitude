const { MessageEmbed } = require('discord.js')
const config = require('../../config')


module.exports = {
    
    name: 'stickerUpdate',
    once: false,

    execute(oldSticker, newSticker) {

       
    }
}