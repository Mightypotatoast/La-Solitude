
const { EmbedBuilder } = require('discord.js')
const config = require('../../config')



module.exports = {
    
    name: 'threadUpdate',
    once: false,

    execute(oldThread, newThread) {

       
    }
}