const { MessageEmbed } = require('discord.js')
const config = require('../../config')


module.exports = {
    
    name: 'roleUpdate',
    once: false,

    execute(oldRole, newRole) {

       
    }
}